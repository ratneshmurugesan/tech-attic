import React from 'react';

import CSSTransitions from 'components/5prototypes/CSSTransitions';

const CSSTransitionsContainer = () => {
  return (
      <CSSTransitions />
  );
};

export default CSSTransitionsContainer;
