import React from 'react';

import ReactQuery from 'components/6micro-apps/react-query';

const ReactQueryContainer = () => {
  return (
      <ReactQuery />
  );
};

export default ReactQueryContainer;
