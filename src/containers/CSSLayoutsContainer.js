import React from 'react';

import CSSLayouts from 'components/5prototypes/CSSLayouts';

const CSSLayoutsContainer = () => {
  return (
      <CSSLayouts />
  );
};

export default CSSLayoutsContainer;
